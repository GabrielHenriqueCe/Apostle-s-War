// As seis faixas, na ORDEM do enum Raridade do C#. É a escada que a alma e o pó compartilham — do
// lado de lá elas são o mesmo tronco (`Material`), e do lado de cá não podem divergir.
//
// O índice que chega pela ponte é o VALOR do enum, nunca a posição numa lista: as listas de preço e
// de queda vêm sem as faixas vazias, então posição não serve de identidade.
export const FAIXAS = ['comum', 'incomum', 'raro', 'epico', 'lendario', 'mitico'];
